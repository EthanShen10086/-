function myPromiseRace(promiseList, timeout, retry = 3) {
	return new Promise((resolve, reject) => {
		let attempt = 0;
		function attempt() {
			attempt++;
			let timeoutPromise = new Promise() 
		}
	});
}
